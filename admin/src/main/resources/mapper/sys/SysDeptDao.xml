<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.base.modules.sys.dao.SysDeptDao">
	
	<select id="queryDetpIdList" resultType="long">
		select dept_id from sys_dept where parent_id = #{value} and del_flag = 0
	</select>
	
	<select id="queryNumber" resultType="long" parameterType="Map">
		select min(number) from sys_dept where del_flag = 0 and show_time = #{showTime} and qrcodeurl is not null and qrcodeurl <![CDATA[<>]]> ''
	</select>
	
	<update id="updateCnt" parameterType="Map">
		update sys_dept
		<set>
			<if test="number != null">
				number = ifnull(number,0) + #{number}
			</if>
		</set>
		where dept_id = #{id}
	</update>

</mapper>